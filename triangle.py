from turtle import *
def triangle(nb_triangle,color):
    # color=str(color)
    for j in range(nb_triangle):
        for i in range(3):
            color("color")
            forward(100)
            left(120)
        color("black")
        forward(120)    
triangle(5)